import React from 'react'

/**
 *     <ObjectWrapper>
 *      <Mesh 
 *			ref={ref}
			le={clicked ? 1.5 : 1}
			lick={(event) => click(!clicked)}
			ointerOver={(event) => hover(true)}
			onPointerOut={(event) => hover(false)}>
 *      >
 *			<GeometryNode geometry={geometry}/>
 *			<Material materialProps={material}/>
 *      </Mesh>
 *      <ObjectWrapper/>
 *
 *
 *
 *
 */

const ObjectWrapper = props => {
	return 'a'
}
